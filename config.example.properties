# Copy this to "config.properties" and fill in real values.
# This file is for LOCAL DEV only. Do NOT commit "config.properties".

# Base URL (optional override)
base_url=https://api.spotify.com/v1

# Spotify App (Client Credentials)
client_id=YOUR_CLIENT_ID
client_secret=YOUR_CLIENT_SECRET

# User token (only needed for user flows)
user_token=YOUR_USER_TOKEN_OPTIONAL